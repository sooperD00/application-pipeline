7/25/2026
application-pipeline\test-vehicles\schema-extraction

---

# startup
- [ ] source .venv/Scripts/activate    # macOS/Linux: source .venv/bin/activate


# git archive just test-vehicles/schema-extraction/
	# [x] Commit first (whatever you want to be in the zip)
	# Preview
	- [x] cd /path/to/ApplicationPipeline/
	- [x] git ls-files test-vehicles/schema-extraction/
	# Zip it
	- [x] git archive -o test-vehicles.zip HEAD test-vehicles/schema-extraction/

# one run
	

---

7/30/2026
## Sprint 0 — Use the Extraction Lab to "Finalize" the Schema
application-pipeline-20260730: test-vehicles/schema-extraction schema finalization

- [x] Housekeeping:
	- [x] duplicate `import prompt as prompt_mod` (`lab.py:37`)
	- [x] stale docstring (`lab.py:2`, says `.txt` only)
	- [x] retired model string (`README.md:84`)
- [x] Token summary line counts cached tokens as spent. Guard on
      `not meta["cached"]`.
	test: you're testing the *report out*, not the resumes,
		so use example-freeform.txt on the cheapest model
	- [x] python lab.py --only example --model claude-haiku-4-5-20251001 # first time
		- [x] 1 fresh / 0 cached      ~404 input tokens to spend
		- [LOOK ->] 2,740 in / 1,980 out tokens spent      1 fresh / 0 cached
	- [x] python lab.py --only example --model claude-haiku-4-5-20251001 --yes # second time
		- [x] 0 fresh / 1 cached      ~0 input tokens to spend
		- [x] 0 in / 0 out tokens spent      0 fresh / 1 cached
	- [x] python lab.py --only example --model claude-haiku-4-5-20251001 --no-cache --yes
		- [x] 1 fresh / 0 cached      ~404 input tokens to spend
		- [x] 2,740 in / 1,925 out tokens spent      1 fresh / 0 cached
	learning:
		the SYSTEM prompt contributes to the overhead, but the compiled 
		JSON schema is actually larger (3,912 characters) and the 4 chars/token 
		ratio is too generous for schema JSON. I should add that to the report 
		and frame it as fixed overhead per call, showing how the schema dominates 
		the per-document cost.

Where the 2,336-token gap goes:
component	chars	in the estimate?
SYSTEM prompt	3,045	no ← you named this
USER_TEMPLATE (fixed part)	222	no
compiled JSON schema	3,912	no
document text	1,619	yes — this is the 404
on the wire	8,798	

The two runs agree, which is the confirmation
Apply the same fixed block to your Opus 5 Profile.pdf run in cost.md:

run	wire chars	input tok	chars/tok
example-freeform, Haiku 4.5	8,798	2,740	3.21
Profile.pdf, Opus 5	13,826	5,286	2.62

Opus 5 produces 23% more tokens for the same text. Anthropic documents that as a real property — models from Claude 4.7 onward use a newer tokenizer that yields roughly 30% more tokens than earlier models, with the exact increase depending on content. Haiku 4.5 predates it; Opus 5 doesn't. Two different documents, two different models, one hypothesis, and the residual is the documented tokenizer delta. That's about as clean as a two-point check gets.

document + fixed (system + schema + template)
404 + 2,760 (1,172 + 1,505 + 85)

	- [-] python lab.py --model claude-haiku-4-5-20251001
	- [x] python lab.py --only example --model claude-haiku-4-5-20251001 --no-cache --yes
		- excellet. yes. the overestimate is bc the estimate multiplier is 
		calculated from claude-opus-5 runs, not haiku.
```
  1 fresh / 0 cached

  document + fixed (system + schema + template)
  623 + 2,761 (1,171 + 1,505 + 85) = ~3,384 input tokens to spend

→ example-freeform (freeform, 1,619 chars)
   coverage  84.4%   buckets  14   unclassified  1   fabricated spans  0  ← LOOK

  name                             est in   actual       Δ      out
  example-freeform                  3,384    2,740    +24%    2,094

2,740 in / 2,094 out tokens spent      1 fresh / 0 cached
```
	- [x] fix: guard for token summary line counted cached tokens as spent
- [x] Dollar estimate in the phase-2 gate. Calibration is already in `cost.md`:
      input ≈ chars/1.25 plus the 3,045-char SYSTEM, output ≈ 2.5× input.
	- [x] A. Prices keyed how? Exact model string ("claude-haiku-4-5-20251001")
	- [x] B. Does the dollar line show a range? ~$0.40 reads well in a gate. $0.33 – $0.48
		- we'll need 3 fields for the tracking csv, lo_, approx_, and hi_
		Range needs a source, and there's a defensible one sitting in your data: 
		you ran both documents twice.
		model / doc	two runs, out-tokens per doc char	spread
		Opus 5 / Profile.pdf	1.89 → 2.24	19%
		Haiku 4.5 / example-freeform	1.19 → 1.22	2.5%
	- [x] C. We need an actual model for output tokens (doc_size * 5 * k) where 5 is bc that's
		how Anthropic prices every model in/out cost multiplier and k because the output is
		larger than the intput as well -- we don't know if it's really a straight multiplier
		but this is our model for now. Calc from the few runs we've done.
	- [x] D. We need a flat file to keep estimates and measurements and other constants in
		-> config.toml for data, typing (including date type), and comments
		-> config.py (toml/pydantic loader) to ingest the "data" from the toml and 
		convert to typed dataclasses
	- [x] dry run (does it run?)
		- [x] python lab.py --dry-run --only example
	- [x] test on a fresh haiku run
		- [x] python lab.py --only example --model claude-haiku-4-5-20251001
			- fixed a divide by zero issue
		- [x] python lab.py --only example --model claude-haiku-4-5-20251001 --no-cache
		- [x] Excellent. The output estimator lands within 2% on its first real check. 
		The input one is the +24% we predicted — Haiku's older tokenizer against a 
		constant measured on Opus.
```
1 fresh / 0 cached

  document + fixed (system + schema + template)
  623 + 2,761 (1,171 + 1,505 + 85) = ~3,384 input tokens
  ~1,943 output tokens

  ~$0.01     claude-haiku-4-5-20251001   $1/$5 per Mtok   (74% of it is output)

spend it?   [Enter] continue   [Ctrl-C] bail
```
name                       est in   actual      Δ  est out   actual      Δ
  example-freeform            3,384    2,740   +24%    1,943    1,980    -2%
```
		- [x] let's print the calculation for ~1,943 output tokens as well
		- [x] ok implementing B. the range, and a dataclass marching towards ledger.csv
			- [x] dry-run, haiku, no-cache # looks good
	- [ ] git commit -m "test-vehicles: dollar estimate in the phase-2 gate

Gate now prints both sides before the spend: document + fixed
(system + schema + template) for input, doc chars × rate for output,
then ~$X.XX at the model's rate.

- fixed block computed from SYSTEM, USER_TEMPLATE and
  Extraction.model_json_schema(), not hard-coded
- CHARS_PER_TOKEN 4 -> 2.6; document tokens stay separate from the
  fixed block so truncation still flags on the document alone
- new Forecast dataclass + forecast() in preview.py — the only place
  an estimate is computed, and the shape of a ledger.csv row
- output carries lo/mid/hi; input stays a point estimate
- prices by exact model string; unpriced model prints ~$? and the run
  continues
- run summary adds est vs actual for both sides, plus whether actual
  landed in range
- config.toml + pydantic loader in config.py, so a typo in a price
  fails at import instead of inside the math. Also absorbs the three
  copies of the 85% coverage floor.

Initial TOML rates from Profile.pdf on Opus 5 and example-freeform on
Haiku 4.5, two runs each. Unmeasured models fall back to a wider
default and say so.

Smoke tested on example-freeform: dry-run, fresh, cached, --no-cache,
and an unpriced model string."



- [ ] `ledger.csv` — append one row per fresh call: timestamp, cache_key, name,
      model, in, out, $, seconds, schema_version, prompt_version
	how to track schema changes in the ledger report? here's an easy version 
	I can track by eye just for development:
		- paste in the header if there isn't one
		- paste in a new header if it doesn't match the old
		- keep track of the "latest" header by pre-pending it to the top of the 
		doc as well as appending it to the start of the latest append section

- [ ] Index table at the top of `report.html` — one row per document. At 30 docs
      the current report is an unnavigable scroll.
- [ ] Integrate the 07-27 observations into schema v2 (§8)
- [ ] Run a document from each band, read the report, then run the corpus

Exit: one schema version survives a document from each band without producing a new
field request, and the suggested titles are good enough to show a stranger.


7/29/2026
application-pipeline-20260729: reporting and schema++ for test-vehicle/schema-extraction

git archive just the test-vehicles/schema-extraction/ folder
	# [x] Commit first (whatever you want to be in the zip)
	# Preview
	- [x] cd /path/to/ApplicationPipeline/
	- [x] git ls-files test-vehicles/schema-extraction/
	# Zip it
	- [x] git archive -o test-vehicles.zip HEAD test-vehicles/schema-extraction/

decision: you need a sprint plan just for this, even:
- [x] git commit -m "docs: plan the remaining v2 sprints

The v2 work has a design session, a cost model, and a working test
vehicle. It has no single place that says what ships in what order.
docs/remaining-sprints.md is that place. Living doc — the dated files
in docs/design/ stay session records, and this supersedes them where
they disagree."

- [ ] Reporting
	- [ ] add how long this took... took maybe 5 min for this 1 run?
	- [ ] understand if the runs are like... one turn at at time? open new context each time? how do multiple resumes get in there
	- [ ] probably don't need SSE streaming at this point ;)
	- [ ] but good job on the intake security bc we'll port that

- [ ] Misleading output:
2. The N in / N out tokens this run line counts tokens replayed from cache as if they were spent.

- [ ] Lint:
3. lab.py:32 — duplicate import prompt as prompt_mod
4. read_samples — warnings = [f"SKIPPED: {e}"] is dead, continue follows it
5. lab.py docstring — "Reads samples/*.txt" (reads pdf/docx now); --model claude-opus-4-8 is a retired model string
6. preview.py — import sys unused

- [ ] Decision pending:
7. DEFAULT_MODEL = "claude-opus-5" — 2.5× Sonnet for a rig meant to run twenty times. Keep, or default to Sonnet and pass --model claude-opus-5 for confirmation runs.

- [ ] The actual next action:
8. Open out/report.html and read the note on that one unclassified bucket from Profile.pdf. 48 buckets, 91% coverage, 0 fabricated — that's a healthy run, and the note is the thing the whole rig exists to produce.


- [ ] One real bug: leftover contradicts itself
	The 07-27 run put a Mosaic Ventures General Counsel role in leftover — 
	but it also bucketed that same role, twice (as two successive titles at one org).
	Your field says "anything you could not place in any bucket at all." The model 
	reported as unplaced something it had placed.
	The newer run's leftover is honest but over-reports the other way — contact lines 
	and "Page 1 of 4" footers, which your prompt already says can be skipped.
	leftover is the least reliable field in the schema. Two runs, two different 
	readings of it. Worth tightening before you trust it as a measurement.

- [ ] Mine and Greg's feedback from session on monday
	includes more return fields

- [ ] resume type categories
	application-pipeline-20260722: Resume categories for ApplicationPipeline user testing)
	did you (greg) capture enough from all the resume categories?
	you probably want to actually decide the names of these categories
	and have ingest agent spit back which one it thinks it belongs in

- [ ] verification order - save for the real thing but
	make sure you take a note and think about this - how to enforce you 
	don't undo the order of things you need them to go it (a dagster type thing)
---

7/29/2026
application-pipeline-20260725: Stand up test-vehicle/schema-extraction

you didn't put in the preview.py report (do it now)
- [x] preview.py
- [x] lab.py updates
- [x] run Profile.pdf again and compare the outputs manually in out/
	- [x] python lab.py --only Profile.pdf  # start 9:12a end 9:12a (this was cached)
	- [x] python lab.py --only Profile.pdf --no-cache  # start 9:22a end 9:23a
HOORAY HOORAY HOORAY!
- [x] compared 2 runs of Profile.pdf through the same model
	- nicole sees no meaninful difference in beyond compare
	- claude sees: 41 quotes appear in both runs. Zero landed in a different bucket type.
- [x] git commit -m "test-vehicles: gate the run behind two human checkpoints

Running the lab meant trusting whatever was in samples/ and finding out
afterward. Two stops, each answering a different question.

Phase 1 — 'did I grab the wrong files'
  Inventory from stat() alone, sorted by bytes descending. No file is
  opened for content, so a stray 50 GB costs microseconds to notice
  rather than however long it takes to parse. Anomalies sort to the
  ends: biggest at top, empty at bottom. Mislabeled files are called
  out here too — 8 magic bytes, not a load.

Phase 2 — 'will it truncate, what will it cost'
  Runs after extraction, which is local and free, so bailing here has
  still spent nothing. Sorted by estimated tokens, with a cache column
  — on a re-run most files cost nothing and that changes the decision.
  Loader warnings collect into one block instead of scrolling past
  while calls are already firing.

Truncation flag is not an arbitrary length. Buckets carry a verbatim
source_quote, so output length tracks input length; a long CV can blow
--max-tokens and truncate, which makes its coverage number meaningless.
Flagged before the spend instead of warned about after.

Also: the API-key check moved above phase 1, so a missing key fails in
half a second instead of after you've read a file list.

--yes skips both checkpoints. --dry-run skips them too — a prompt that
fires when there's nothing at stake is friction you'd route around."
- [x] git push # and time for new chat

---

7/27/2026
application-pipeline-20260725: Stand up test-vehicle/schema-extraction
Test on real content

- [x] python lab.py --only Profile.pdf  # start 9:12a end END_TIME


```
$ python lab.py --only Profile.pdf
→ Profile (unknown, 6,647 chars)
   coverage  91.0%   buckets  48   unclassified  1   fabricated spans  0  ← LOOK

5,286 in / 14,882 out tokens this run (cached calls cost nothing).
Report: C:\Users\Barac0da\source\repos\application-pipeline\test-vehicles\schema-extraction\out\report.html
```

---

# 7/25/2026 1:12p
application-pipeline-20260725: Stand up test-vehicle/schema-extraction
Add loaders.py; ALLOWED_FILE_TYPES = {".txt", ".md", ".pdf", ".docx"}

- [x] loaders.py
- [x] requirements.txt
```text
pdfplumber>=0.11      # PDF text, layout-aware
python-docx>=1.2      # .docx text
```
- [x] lab.py - import block
```
import loaders
import prompt as prompt_mod
```
- [x] lab.py - replace read_samples()
- [x] python lab.py --dry-run --only example-freeform.txt
- [x] git commit -m "test-vehicles: read pdf and docx samples, not just .txt

Most of the corpus is PDF. loaders.py extracts text before the API
call; nothing downstream changed.

- type detected from magic bytes, not extension — a PDF named .docx
  is a real thing people send
- docx walks body children rather than doc.paragraphs, which silently
  skips tables; a table-layout resume returned 10 chars before this
- scanned PDFs raise LoadError and are skipped with a reason, rather
  than passing through as empty-two-quotes string and scoring 0% coverage"


# 7/25/2026 12:04p 
application-pipeline-20260725: Stand up test-vehicle/schema-extraction

Set up venv for this test vehicle

- nexted venvs don't fight each other
- python -m venv writes an absolute path into its own scripts
git bash
- [-] cd /c/Users/Barac0da/source/repos/application-pipeline/test-vehicles/schema-extraction
- [-] deactivate 2>/dev/null   # in case the main venv is live in this tab
- [x] python -m venv .venv  # creates a .venv folder (takes a hot min)
- [x] add .venv to local .gitignore
- [x] source .venv/Scripts/activate
- [x] python -m pip install -r requirements.txt
- [-] deactivate # when you're done
anthropic key
- [x] ensure .env is in .gitignore
- [x] touch .env .env.example
	- ANTHROPIC_API_KEY=sk-ant-your-key-here
- [x] git commit -m "test-vehicles: make schema-extraction self-contained

The folder now stands up on its own so it can be picked up cold after
a break, or cloned somewhere else entirely:

- venv lives in the folder, not shared with backend/ — plain venv +
  requirements.txt, no pyproject so nothing walks up the tree
- lab.py calls load_dotenv(), so the key comes from .env instead of a
  manual export that dies with the terminal tab
- .env.example committed as the template; .env ignored
- samples/ and out/ ignored (both hold real resume text); one example
  sample kept as a reference
- README updated
- dry run with samples/example-freeform.txt works"


# 7/24/2026
application-pipeline-20260724-1459: Feedback Scaffold and Phase 0 Notes

decide the folder structure in 
application-pipeline
	|- backend/
	|- frontend/
	|- test-vehicles/schema-extraction   <---------- this one
		|- .gitignore  # local .gitignore - don't commit PPI
	|- .dockerignore  # don't spin up your test folder in prod


# 7/20/2026
application-pipeline-20260720: Designing onboarding process - python test vehicle and html viewer

actually design the test vehicle